# Define observed objects in a set
def observed():
  observations = {"Flying Car", 
                  "Sky Scraper", 
                  "Laser", 
                  "Dome"}
  # also works with: observations = set([list of items])

  return observations

# Print the oberseved function
def run():
  print(observed())

# Function call for run
run()